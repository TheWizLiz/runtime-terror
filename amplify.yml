version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm start
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory:build
    files:
      - '**/*'
  cache:
    paths: []
